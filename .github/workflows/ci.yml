name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependências (sem GTK)
        run: |
          pip install ruff pytest pytest-cov pytest-timeout
          pip install requests beautifulsoup4 lxml
          pip install opencv-python Pillow numpy scikit-learn httpx python-dotenv
          pip install cairosvg

      - name: "1/4 — Lint"
        run: |
          ruff check src/ tests/
          ruff format --check src/ tests/

      - name: "2/4 — Smoke"
        run: pytest tests/smoke/ -v --timeout=10

      - name: "3/4 — Unit"
        # Exclui testes marcados como requires_display: dependem de PyGObject
        # (gi.repository.GLib) que so existe no ambiente do desktop.
        run: pytest tests/unit/ -v -m "not requires_display"

      - name: "4/4 — Verificação de anonimato"
        run: |
          if grep -r "Claude\|ChatGPT\|Gemini\|Copilot\|Anthropic\|OpenAI" src/ --include="*.py" 2>/dev/null; then
            echo "FALHA: Menção a IA detectada no código"
            exit 1
          fi
          echo "Anonimato OK"
